---
label: pull quote and paragraph
hide_body: false
is_partial: true
fields:
- type: textarea
  label: main content
  name: main_content
  config:
    wysiwyg: true
    schema:
      format: markdown
- type: textarea
  name: quote_content
  label: quote content
  config:
    wysiwyg: true
    schema:
      format: markdown
- type: number
  name: quote_offset
  label: quote offset
  config:
    min: '1'
    step: '1'
- type: select
  config:
    source:
      type: simple
    options:
    - left
    - right
  label: quote alignment
  name: quote_alignment
